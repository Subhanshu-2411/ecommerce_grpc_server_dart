name: ecommerce_grpc_server_dart
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.2

dependencies:
  grpc: ^3.1.0
  protobuf: ^2.1.0

dev_dependencies:
  http: ^0.13.6
  lints: ^2.0.0
  test: ^1.21.0
